🎉 webstack-go has been successfully deployed!

📋 Release Information:
  Release Name: {{ .Release.Name }}
  Namespace: {{ .Release.Namespace }}
  Chart Version: {{ .Chart.Version }}
  App Version: {{ .Chart.AppVersion }}

🔍 To check the status of your deployment:
  kubectl get statefulset {{ include "nav.fullname" . }} -n {{ .Release.Namespace }}
  kubectl get pods -l "app.kubernetes.io/name={{ include "nav.name" . }}" -n {{ .Release.Namespace }}

📊 To view logs:
  kubectl logs -f statefulset/{{ include "nav.fullname" . }} -n {{ .Release.Namespace }}

🌐 To access your application:

{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}
  🔗 URL: http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
  {{- end }}
{{- end }}
{{- else if contains "NodePort" .Values.service.type }}
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "nav.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo "🔗 URL: http://$NODE_IP:$NODE_PORT"
{{- else if contains "LoadBalancer" .Values.service.type }}
  NOTE: It may take a few minutes for the LoadBalancer IP to be available.
  export SERVICE_IP=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "nav.fullname" . }} --template "{{"{{ range (index .status.loadBalancer.ingress 0) }}{{.}}{{ end }}"}}")
  echo "🔗 URL: http://$SERVICE_IP:{{ .Values.service.port }}"
{{- else if contains "ClusterIP" .Values.service.type }}
  🔧 Port-forward to access locally:
  kubectl port-forward --namespace {{ .Release.Namespace }} svc/{{ include "nav.fullname" . }} 8000:{{ .Values.service.port }}
  echo "🔗 URL: http://127.0.0.1:8000"
{{- end }}

📚 API Documentation:
  Access Swagger UI at: http://your-domain/swagger/index.html

{{- if .Values.persistence.enabled }}
💾 Persistence:
  Storage is enabled with {{ .Values.persistence.size }} volume
  {{- if .Values.persistence.existingClaim }}
  Using existing PVC: {{ .Values.persistence.existingClaim }}
  {{- else }}
  A new PVC will be created for each pod
  {{- end }}
{{- else }}
⚠️  WARNING: Persistence is disabled! Data will be lost on pod restart.
{{- end }}

{{- if .Values.autoscaling.enabled }}
⚡ Auto-scaling:
  Enabled with min={{ .Values.autoscaling.minReplicas }} and max={{ .Values.autoscaling.maxReplicas }} replicas
{{- end }}

🔐 Security Notes:
{{- if eq .Values.config.data.security.jwt.key "CHANGE_ME_IN_PRODUCTION" }}
  ⚠️  WARNING: Default JWT key detected! Please update it for production!
{{- end }}

📖 For more information:
  Visit: https://github.com/ch3nnn/webstack-go
